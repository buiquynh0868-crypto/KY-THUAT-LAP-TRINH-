#include <stdio.h>
#include <math.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	float a , b; 
	float S; 
	
	printf ("Nhap so thuc a: "); scanf ("%f", &a); 
	printf ("Nhap so thuc b: "); scanf ("%f", &b); 
	
	S = (a + b)*(a + b); 
	
	printf ("\n Tong binh phuong S =%2f \n", S);  
	
	
	return 0;
}
